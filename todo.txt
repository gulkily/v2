things that need to be folded into the dev process:

- sqlite index

- clear instructions for participation for agents

- comparison of architecture documents and checklist, to identify anything planned in one place but missing or only implied in the other

- on current page, there should be a textarea which goes to an llm, including the current page's content/context with the query
	- uses dedalus as backend
	- displays results in-page with js if possible
	- content is retained on the page for the present user
		- how?
		- can be published  to the public as a comment on the page

- a script to scan the threads status and add comments
	- criteria for adding comments:
		- thread has been without any comments for a certain amount of time (e.g. 24 hours)
		- thread contains content that is inaccurate or should be corrected
		- moderation-actionable events (e.g. spam, harassment, etc.)

- moderation tools:
	- flagging system for users to report inappropriate content
	- automated detection of spam or harmful content using machine learning models
	- a dashboard for moderators to review flagged content and take action (e.g. remove, warn user, etc.)


- script to import all of a subreddit into the system, including threads, comments, and metadata (e.g. author, timestamp, etc.)
	- should be able to handle large volumes of data efficiently
	- should be able to update the database with new threads and comments as they are posted on the subreddit
	- should be able to limit the import to a specific time range or set of threads (e.g. only threads with a certain number of upvotes, etc.)

- update grammar of system when possible
	- directories, database entries, urls, etc, should be singular, not plural.
	- e.g. "thread" instead of "threads", "comment" instead of "comments", etc.

- compose page should have auto-save, so that the user doesn't lose what they typed.
	- auto-save should save the content to local storage or a temporary database entry, so that it can be retrieved if the user accidentally navigates away from the page or closes the browser.
	- auto-save should also have a timestamp, so that the user can see when their content was last saved.

- a page that communicates the node configuration settings
	- e.g. retention policy, moderation settings, etc.
	- admin id, contact information, etc.
	- should be easily accessible from the main page, and should be updated in real-time as changes are made to the configuration settings.
	- commit id, date, installation date, etc.

- more robust local dev server
	- start on a "v2" command
	- detect changes in running code, and reload
	- ^c handler

- frontend checklist
	- dark mode toggle

- git log on website somewhere
	- page with newest content feed together with git log
	- general event log, which includes moderation log, posts, replies, other filters

- generate some test moderation data

- how do i view user's public key from profile page?

- on compose reply page, I want to see the content I'm replying to for reference.

- when importing gpg key, add provision for this happening:
	- Signed posting failed: Error signing message: Signing key is not decrypted.

- bring todo.txt and ideas.txt to the web interface
	- generally make code more accessible and transparent
		- as a pathway to self-modification api

- review unanswered posts with a question and reply to the best of my ability.

- key manager. do not discard previous key when generating new one.

- user directory

- recognize hashtags

- implement proof of work button as a captcha

- root mod should be able to grant moderator privileges to other users,
	- there should be a page for managing moderators and their permissions.
	- other mods should be able to grant mod to other users.
	- the whole tree of mod-granting should be visible on the site, so that users can see who granted mod to whom, and who the root mod is.
	- removing mod privileges from a user should also remove any mod privileges that they granted to other users, and this should also be reflected in the mod tree.

- what's the easiest way to host this?

- on the compose thread page, where the keys etc are displayed, make the data monospace font

- change command name from forum to v2

- add dedalus api support and .env

- zip file export of a profile, tag, thread, or any other listable collection of content, including all relevant metadata and content in a structured format (e.g. JSON, markdown, etc.)
	- should be able to select which content to include in the export (e.g. only threads with a certain tag, only threads created by a certain user, etc.)
	- should be able to import the zip file back into the system, restoring all content and metadata as it was at the time of export.
	- the zip file format should be text files with a clear and consistent structure, so that it can be easily parsed and understood by both humans and machines.

- if user is submitting non-ascii text, do some basic attempts
	- convert single quotes and double quotes to ascii equivalents
	- convert common punctuation to ascii equivalents
	- if the text contains a high percentage of non-ascii characters, display a warning to the user that their content may not be displayed correctly on all devices or platforms, and suggest that they use ascii characters where possible for better compatibility.

- when a new thread is created, automatically use llm to assess its 0-1 zen value.
	- this can be done by sending the thread content to an llm with a prompt that asks it to evaluate the zen value of the thread on a scale of 0 to 1, where 0 is not zen at all and 1 is extremely zen.
	- the resulting zen value can then be stored in the database and displayed on the thread page, allowing users to quickly assess the zen quality of a thread before reading it.

- add a queue system for async processes

- fix display of thread titles wherever they're presented as the slug rather than the title

- user directory