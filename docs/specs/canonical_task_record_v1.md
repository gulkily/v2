# Canonical Task Record V1

This spec is historical only.

The `thread_native_task_roots` feature superseded the separate `records/tasks/`
record family. Current planning state now lives on root posts in `records/posts/`
with `Thread-Type: task`, documented in
[canonical_post_record_v1.md](/home/wsl/v2/docs/specs/canonical_post_record_v1.md).

Keep this file only as a reference for the earlier experiment; do not add new
canonical planning records under `records/tasks/`.
