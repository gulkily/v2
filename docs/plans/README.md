# Planning Docs

Most feature-planning artifacts still live directly in this directory.

When a feature grows to four or more planning artifacts, move it into a
dedicated subdirectory so the Step 1-4 trail stays grouped together.

Current grouped feature folders:

- `thread_native_task_roots/`
