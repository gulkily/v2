# Moderation Records

Canonical moderation records live in this directory.

Moderation actions are stored separately from post content so visible-state changes remain auditable in git-backed text files.
