# Identity Link Records

This directory is reserved for canonical signed identity-link records.

Planned link actions for the current implementation slice:

- `rotate_key`
- `merge_identity`

Each accepted record is stored as an ASCII text payload with detached signature and public-key sidecars.
