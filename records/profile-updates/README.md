# Profile Update Records

This directory is reserved for canonical signed profile-update records.

Planned profile-update actions for the current implementation slice:

- `set_display_name`

Each accepted record is stored as an ASCII text payload with detached signature and public-key sidecars.
